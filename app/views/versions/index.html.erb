<div class="container">

  <h3> <%= @versions.first.title %>: Revision History </h3>

  <ul>
    <% @versions.each do |version| %>
      <li><%= link_to version.title , version_path(version) %> - <%= version.created_at.strftime('%B %e, %Y, %l:%M %p') %>  </li>
    <% end %>
  </ul>

</div>

